{
  "name": "proto",
  "version": "1.0.0",
  "description": "",
  "main": "src/park_map.html",
  "scripts": {
    "start": "npm run start-src & npm run start-static",
    "start-src": "http-server ./src -p 8080 --cors",
    "start-static": "http-server ./static -p 8081 --cors",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
